{% extends 'base.html.twig' %}

{% block title %}ServicePricing index{% endblock %}

{% block body %}
    <h1>ServicePricing index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>StartDate</th>
                <th>EndDate</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for service_pricing in service_pricings %}
            <tr>
                <td>{{ service_pricing.id }}</td>
                <td>{{ service_pricing.startDate ? service_pricing.startDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ service_pricing.endDate ? service_pricing.endDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ service_pricing.price }}</td>
                <td>
                    <a href="{{ path('service_pricing_show', {'id': service_pricing.id}) }}">show</a>
                    <a href="{{ path('service_pricing_edit', {'id': service_pricing.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('service_pricing_new') }}">Create new</a>
{% endblock %}
