{% extends 'base.html.twig' %}

{% block title %}Appartment{% endblock %}

{% block body %}
    <h1>Appartment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appartment.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ appartment.description }}</td>
            </tr>
            <tr>
                <th>NumberOfRooms</th>
                <td>{{ appartment.numberOfRooms }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ appartment.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('appartment_index') }}">back to list</a>

    <a href="{{ path('appartment_edit', {'id': appartment.id}) }}">edit</a>

    {{ include('appartment/_delete_form.html.twig') }}
{% endblock %}
